[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "vigia-rh"
version = "1.0.0"
description = "Sistema de Analitica Predictiva de Rotacion de Personal"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]